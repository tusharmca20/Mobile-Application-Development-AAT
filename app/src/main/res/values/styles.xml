
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Light" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="cardBackgroundColor">@android:color/white</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
        <item name="popupMenuStyle">@style/ThemeOverlay.AppCompat.Light</item>
    </style>

    <style name="AppTheme.Dark" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="popupMenuStyle">@style/ThemeOverlay.AppCompat.Dark</item>
    </style>

    <style name="AppTheme.Black" parent="AppTheme.Dark">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="cardBackgroundColor">@color/grey</item>
        <item name="md_background_color">@color/grey</item>
        <item name="popupMenuStyle">@style/OptionsMenuBlack</item>
    </style>

    <style name="OptionsMenuBlack" parent="@style/ThemeOverlay.AppCompat.Dark">
        <item name="android:itemBackground">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <style name="AppTheme.Light.NoActionBar" parent="AppTheme.Light">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Dark.NoActionBar" parent="AppTheme.Dark">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Black.NoActionBar" parent="AppTheme.Black">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="SplashTheme" parent="AppTheme.Light">
        <item name="android:windowBackground">@drawable/launch_screen</item>
    </style>
</resources>
